apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mcart-tls
  namespace: mcart-gateway
spec:
  secretName: mcart-tls
  issuerRef:
    name: letsencrypt-gateway
    kind: ClusterIssuer
  dnsNames:
    - mcart.space
    - www.mcart.space

