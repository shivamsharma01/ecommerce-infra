apiVersion: v1
kind: Service
metadata:
  name: email
  labels:
    app: email
spec:
  type: ClusterIP
  selector:
    app: email
  ports:
    - name: http
      port: 8089
      targetPort: http
      protocol: TCP
