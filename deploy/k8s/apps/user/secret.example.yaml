# Copy to secret.yaml (gitignored), replace MY_PASSWORD, then: kubectl apply -n mcart -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: user-secrets
  labels:
    app: user
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: MY_PASSWORD
