apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app.kubernetes.io/name: payment
    app: payment
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payment
    app: payment
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
