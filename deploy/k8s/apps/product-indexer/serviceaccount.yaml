# Annotate for Workload Identity after you create the GCP service account:
#   iam.gke.io/gcp-service-account: mcart-product-indexer@PROJECT_ID.iam.gserviceaccount.com
apiVersion: v1
kind: ServiceAccount
metadata:
  name: product-indexer
  labels:
    app.kubernetes.io/name: product-indexer
    app.kubernetes.io/version: "0.0.1"
