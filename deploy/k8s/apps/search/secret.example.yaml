# Copy to secret.yaml (gitignored). Needed only if OpenSearch has the security plugin enabled.
apiVersion: v1
kind: Secret
metadata:
  name: search-secrets
  labels:
    app: search
type: Opaque
stringData:
  OPENSEARCH_USERNAME: admin
  OPENSEARCH_PASSWORD: CHANGE_ME
