apiVersion: v1
kind: Service
metadata:
  name: search
  labels:
    app: search
spec:
  type: ClusterIP
  selector:
    app: search
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
