apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mcart-bootstrap.fullname" . }}-payment-flyway-sql
  namespace: {{ .Values.namespace }}
data:
{{- range $path, $_ := .Files.Glob "files/payment/*.sql" }}
  {{ base $path }}: |
{{ $.Files.Get $path | nindent 4 }}
{{- end }}
